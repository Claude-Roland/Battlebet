name: battlebet_server
description: BattleBet backend — Konten, Wallet (Test-Credits), Wetten, Läufe, Ledger.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  dart_frog: ^1.1.0
  postgres: ^3.0.0
  bcrypt: ^1.1.3

dev_dependencies:
  dart_frog_lint: ^0.1.0
  mocktail: ^1.0.0
  test: ^1.0.0
